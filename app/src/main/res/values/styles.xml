<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="BaseView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minWidth">40dp</item>
        <item name="android:minHeight">30dp</item>
        <item name="android:layout_margin">2dp</item>
        <item name="android:paddingLeft">5dp</item>
        <item name="android:paddingRight">5dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">11sp</item>
        <item name="android:textColor">@color/public_button_text_color</item>
    </style>

    <style name="Button" parent="BaseView">
        <item name="android:background">@drawable/public_button</item>
    </style>

    <style name="TextView" parent="BaseView">
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="EditText" parent="BaseView">
        <item name="android:gravity">center_vertical</item>
        <item name="android:minWidth">100dp</item>
    </style>

    <!--//////////////////////////////////////////////////////////////////////////////-->
    <style name="BaseLayout">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="LayoutHorizontal" parent="BaseLayout">
        <item name="android:orientation">horizontal</item>
    </style>

    <style name="LayoutVertical" parent="BaseLayout">>
        <item name="android:orientation">vertical</item>
    </style>

</resources>